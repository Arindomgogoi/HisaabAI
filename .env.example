# Copy this file to .env.local and fill in real values
# For production (Vercel): set these in Vercel → Project → Settings → Environment Variables

# Neon PostgreSQL connection string
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# NextAuth (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="generate-a-random-32-char-secret-here"
AUTH_SECRET="generate-a-random-32-char-secret-here"

# Local dev:
NEXTAUTH_URL="http://localhost:3000"
# Production (set in Vercel dashboard, not here):
# NEXTAUTH_URL="https://hisaab-ai.vercel.app"

# Claude AI
ANTHROPIC_API_KEY="sk-ant-your-key-here"
